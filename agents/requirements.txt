# agents/requirements.txt
# Python dependencies for DriftWatch AI agents (Azure Functions)
#
# Core: Semantic Kernel SDK for agent orchestration (Planner → Skills → Memory)
# Hero Technologies: Azure AI Foundry, OpenTelemetry, MCP Server, RAG, AI Language, Blob Storage

# --- Azure Functions Runtime ---
azure-functions

# --- Azure Identity & AI Foundry ---
azure-identity
azure-ai-projects>=1.0.0b1           # Azure AI Foundry SDK (Responses API, agent registration)

# --- LLM Client ---
openai>=1.12.0

# --- Microsoft Agent Framework (Semantic Kernel) ---
semantic-kernel>=1.0.0

# --- HTTP Client ---
httpx>=0.27.0

# --- Data Validation ---
pydantic>=2.0.0

# --- Environment ---
python-dotenv

# --- OpenTelemetry (gen_ai.* spans → Application Insights → Foundry Operate tab) ---
opentelemetry-sdk>=1.20.0
opentelemetry-api>=1.20.0
azure-monitor-opentelemetry-exporter>=1.0.0b21
opentelemetry-instrumentation-openai>=0.1.0

# --- Azure MCP Server (Model Context Protocol) ---
fastmcp>=0.1.0

# --- Azure AI Content Safety ---
# (Uses REST API via httpx — no additional SDK needed)

# --- Azure AI Language (Key Phrase Extraction + Entity Recognition) ---
azure-ai-textanalytics>=5.3.0

# --- Azure AI Search — RAG (Retrieval-Augmented Generation) ---
azure-search-documents>=11.4.0

# --- Azure Blob Storage — MRP Artifact Archiving ---
azure-storage-blob>=12.19.0
